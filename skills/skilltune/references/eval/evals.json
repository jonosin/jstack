{
  "skill": "skilltune",
  "question": "Does an agent following the (mutated) skilltune SKILL.md + tune-mode.md open dashboard.html ITSELF at S1, deterministically, in both Auto and Show-eval modes?",
  "booleans": {
    "opens_dashboard": "plan contains an actual open command targeting dashboard.html",
    "opens_at_s1": "the agent's own open happens ASAP at S1 (right after first generation), not deferred to S6/end",
    "deterministic_open": "the AGENT runs a concrete open/xdg-open/start command itself; NOT 'tell Jono / ask the human to open it'",
    "auto_mode_no_human": "opens without relying on telling/waiting on any human",
    "survives_teardown": "plan keeps the dashboard viewable post-run (already-open tab stays frozen; not deleted before the report is persisted)"
  },
  "probes": [
    {
      "id": "auto_goal_basic",
      "split": "train",
      "session_context": "You are an agent invoked under `/goal` (Auto mode active, no human watching). The goal: tune the skill `jstack-foo` for accuracy.",
      "user_request": "/goal /skilltune jstack-foo",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human", "survives_teardown"]
    },
    {
      "id": "auto_arg",
      "split": "train",
      "session_context": "Attended session but invoked with the explicit `auto` arg, so the loop runs end-to-end autonomously.",
      "user_request": "/skilltune jstack-foo auto",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human"]
    },
    {
      "id": "show_eval_default",
      "split": "train",
      "session_context": "Bare invocation, no /goal, no auto arg — Show-eval mode. The eval set has just been approved and you are entering the S1/setup dashboard-launch step.",
      "user_request": "/skilltune jstack-foo",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open"]
    },
    {
      "id": "loop_goal",
      "split": "train",
      "session_context": "Unattended long run wrapped in /loop under /goal; nobody is at the keyboard.",
      "user_request": "/loop 30m /goal /skilltune jstack-foo",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human", "survives_teardown"]
    },
    {
      "id": "control_no_dash_mention",
      "split": "train",
      "session_context": "A normal attended tune. The user never mentions the dashboard at all; you are at the S1 dashboard-launch step after recording the baseline.",
      "user_request": "tune my jstack-foo skill, make it more accurate",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open"]
    },
    {
      "id": "s1_first_generation",
      "split": "train",
      "session_context": "Auto run. You have just written the initial history.json and run dashboard.py for the very first time — dashboard.html now exists in the sandbox. Describe exactly what you do next regarding that file.",
      "user_request": "(internal S1 step — dashboard.html just generated)",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human"]
    },
    {
      "id": "finished_asks_to_see",
      "split": "heldout",
      "session_context": "An Auto tune run has just finished its S5 stop. The user now asks to see the dashboard. Describe what your plan must have done so this request can be satisfied.",
      "user_request": "show me the dashboard for that run",
      "expect": ["opens_dashboard", "survives_teardown", "deterministic_open"]
    },
    {
      "id": "macos_platform",
      "split": "heldout",
      "session_context": "Auto run on macOS (darwin). You are at the S1 dashboard-launch step.",
      "user_request": "/goal /skilltune jstack-foo",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human"]
    },
    {
      "id": "show_eval_handoff",
      "split": "heldout",
      "session_context": "Show-eval evals were approved earlier; the loop is now running in a FRESH unattended session handed off via /goal. You are at the S1 dashboard-launch step.",
      "user_request": "/goal Continue the skilltune for jstack-foo from the approved sandbox; run the S3 loop.",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human"]
    },
    {
      "id": "unattended_cron",
      "split": "heldout",
      "session_context": "Fully unattended cron invocation (`claude -p`). No human will ever see a 'tell Jono' instruction during this run. You are at the S1 dashboard-launch step.",
      "user_request": "claude -p \"/goal /skilltune jstack-foo\"",
      "expect": ["opens_dashboard", "opens_at_s1", "deterministic_open", "auto_mode_no_human", "survives_teardown"]
    }
  ]
}
