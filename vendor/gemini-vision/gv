#!/usr/bin/env bash
# gv — generic Gemini multimodal call via Vertex AI.
# Wraps the official `gemini` CLI (google-gemini/gemini-cli) with auth,
# project, and file-include defaults so any agent can call it cleanly.
#
# Auth: Vertex AI + cached OAuth, no API key.
# Billed to the project's billing account (consumes GCP credits).
#
# Usage:
#   gv -p "describe this" @~/pic.jpg
#   gv -m gemini-2.5-pro -p "..." @file.png
#   gv -p "transcribe" @~/recording.mp3
#   gv -p "what happens" @~/clip.mp4
#   gv -p "summarize" @~/doc.pdf
#   gv -p "compare these" @a.jpg @b.jpg
#   echo "describe this" | gv -p -
#
# Config: GOOGLE_CLOUD_PROJECT et al. come from the environment or
# ~/.jstack/config.env. Set GOOGLE_CLOUD_PROJECT to your own GCP project.
#
# Env overrides:
#   GEMINI_VISION_MODEL  model name (default: gemini-2.5-flash)
#   GOOGLE_CLOUD_PROJECT project ID (required — your GCP project)
#   GOOGLE_CLOUD_LOCATION region    (default: us-central1)
#   GEMINI_CLI_TRUST_WORKSPACE  trust cwd non-interactively (default: true)
set -euo pipefail

# Load jstack config if present (does not override values already in the env).
if [ -f "$HOME/.jstack/config.env" ]; then
  set -a; . "$HOME/.jstack/config.env"; set +a
fi

export PATH="$HOME/.local/bin:$PATH"
export GOOGLE_GENAI_USE_VERTEXAI=true
export GOOGLE_CLOUD_PROJECT="${GOOGLE_CLOUD_PROJECT:-}"
export GOOGLE_CLOUD_LOCATION="${GOOGLE_CLOUD_LOCATION:-us-central1}"

if [ -z "$GOOGLE_CLOUD_PROJECT" ]; then
  echo "error: GOOGLE_CLOUD_PROJECT is not set. Export it or add it to ~/.jstack/config.env." >&2
  exit 1
fi
# gemini-cli >=0.45 refuses to run non-interactively in an "untrusted" directory.
# This wrapper is invoked by agents that already chose the cwd, so trust it.
export GEMINI_CLI_TRUST_WORKSPACE="${GEMINI_CLI_TRUST_WORKSPACE:-true}"

MODEL="${GEMINI_VISION_MODEL:-gemini-2.5-flash}"

if ! command -v gemini >/dev/null 2>&1; then
  echo "error: 'gemini' CLI not found on PATH. Install: brew install gemini-cli" >&2
  exit 1
fi

exec gemini \
  -m "$MODEL" \
  --approval-mode default \
  --include-directories "$(pwd)" \
  "$@"
